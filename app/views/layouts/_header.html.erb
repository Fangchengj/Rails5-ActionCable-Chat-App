<header>
    <h4>
      <% if user_signed_in? %>
      Logged in as, <%= current_user.name %> - <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
      <% else %>
      <%= link_to "login", new_user_session_path %>
      <%= link_to "sign up", new_user_registration_path %>
      <% end %>
    </h4>
</header>
